# NPC State Machine

A lightweight state machine module and a set of guard NPCs that use it to patrol between flags, idle, and attack, showing how to compose transition tests with `AND`, `OR`, and `NOT`.

Companion code for https://www.youtube.com/watch?v=nlVpAZEkxgo
